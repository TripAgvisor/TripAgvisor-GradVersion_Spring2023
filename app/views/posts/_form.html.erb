<%= form_for(@post, :html => { :multipart => true }) do |f| %>
   <div class="field">
     <%= f.label :title %><br>
     <%= f.text_field :title %>
   </div>
   <%= f.fields_for :post_attachments do |p| %>
     <div class="field">
       <%= p.label :images %><br>
       <%= p.file_field :images, :multiple => true, name: "post_attachments[images][]" %>
     </div>
   <% end %>
   <div class="actions">
     <%= f.submit %>
   </div>
<% end %>